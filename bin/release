#!/usr/bin/env bash

# bin/release <build-dir>

cat <<EOF
config_vars:
  MYENV: foo
  PATH: \$HOME/vendor/shellinabox/bin:\$PATH
default_process_types:
  web: \$HOME/vendor/shellinabox/bin/shellinaboxd -p \$PORT
EOF