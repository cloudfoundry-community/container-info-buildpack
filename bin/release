#!/usr/bin/env bash

# bin/release <build-dir>

cat <<EOF
config_vars:
  PATH: bin:node_modules/.bin:/usr/local/bin:/usr/bin:/bin:\$PATH
default_process_types:
  web: app/bin/cloud9.sh -l 0.0.0.0 -p \$PORT 
EOF